# Copier en `.env.local` (ignoré par git).

VITE_API_URL="http://localhost:8787"

# Clerk — clé publique. Si elle est absente, l'application tourne sans comptes :
# on peut jouer en local, mais personne ne peut être abonné.
VITE_CLERK_PUBLISHABLE_KEY="pk_test_..."
